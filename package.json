{
  "name": "salt-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI coding agent with Vercel AI SDK, HTTP server for IM integration and Web UI",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @salt-agent/server",
    "typecheck": "npm run typecheck -w @salt-agent/core && npm run typecheck -w @salt-agent/server",
    "db:generate": "npm run db:generate -w @salt-agent/core",
    "db:migrate": "npm run db:migrate -w @salt-agent/core"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
