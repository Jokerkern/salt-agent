{
  "name": "salt-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI coding agent with Vercel AI SDK, HTTP server for IM integration and Web UI",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start -w @salt-agent/salt-agent",
    "dev": "npm run dev -w @salt-agent/salt-agent",
    "dev:web": "npm run dev -w @salt-agent/web",
    "build:web": "npm run build -w @salt-agent/web",
    "typecheck": "npm run typecheck -w @salt-agent/salt-agent"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
